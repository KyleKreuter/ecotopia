spring:
  datasource:
    url: jdbc:h2:mem:ecotopia-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  ai:
    openai:
      api-key: test-key
      base-url: https://api-inference.huggingface.co/v1
      chat:
        options:
          model: test-model
